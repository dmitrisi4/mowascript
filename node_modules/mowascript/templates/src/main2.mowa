мур імя = "Дзіма";
скажы("Прывітанне, " + імя + "!");

калі (імя == "Дзіма") {
  скажы("Ты аўтар моваскрыпта!");
} іначай {
  скажы("Хто ты?");
}

канстанта адказ = новы Абяцанне((выканац, адхілі) => {
  вярнуць("Усё гатова!");
});

адказ
  .патым((вынік) => {
    скажы("Вынік: " + вынік);
  })
  .злаві((памылка) => {
    скажыПамылка(памылка);
  })
  .нарэшце(() => {
    скажыІнфа("Завяршэнне");
  });


  канстанта лічбы = [1, 2, 3, 4, 5];

  канстанта фільтраваныя = лічбы.адфільтруй((н) => {
    вярнуць н % 2 == 0;
  });

  скажы(фільтраваныя);