мур імя = "Дзіма";
скажы("Прывітанне, " + імя + "!");

калі (імя == "Дзіма") {
  скажы("Ты аўтар моваскрыпта!");
} іначай {
  скажы("Хто ты?");
}

канстанта адказ = новы Абяцанне((выканац, адхілі) => {
  вярнуць("Усё гатова!");
});

адказ
  .патым((вынік) => {
    скажы("Вынік: " + вынік);
  })
  .злаві((памылка) => {
    скажыПамылка(памылка);
  })
  .нарэшце(() => {
    скажыІнфа("Завяршэнне");
  });


  канстанта лічбы = [1,2,2, 5,2, 32, 4, 5, 6, 7, 2];

  канстанта фільтраваныя = лічбы.адфільтруй((н) => {
    вярнуць н % 2 == 0;
  });

  скажы(фільтраваныя);

// Украинский (mowa-ua)

  функція привітання(імʼя) {
    кажи("Привіт, " + імʼя + "!");
  }

  нехай імена = ["Андрій", "Оля", "Марко"];

  імена.кожен((імʼя) => {
    привітання(імʼя);
  });

  // Белорусский (mowa-by)
  функцыя прывітанне(імя) {
    скажы("Прывітанне, " + імя + "!");
  }

  нехай імены = ["Янка", "Настасся", "Кастусь"];

  імены.дляКожнага((імя) => {
    прывітанне(імя);
  });

  ///Русский (mowa-ru)
  функция приветствие(имя) {
    лог("Привет, " + имя + "!");
  }

  пусть имена = ["Ваня", "Катя", "Петя"];

  имена.каждому((имя) => {
    приветствие(имя);
  });

// (mowa-isv)
  funkcija pozdrav(ime) {
    govori("Zdravstvuj, " + ime + "!");
  }

  nehaj imena = ["Milan", "Lena", "Radek"];

  imena.dlaKaždogo((ime) => {
    pozdrav(ime);
  });

нехай вік = 22220;

якщо (вік >= 18) {
  кажи("Доступ дозволено");
} інакше {
  кажи("Доступ заборонено");
}

нехай узрост = 20;

калі (узрост >= 18) {
  скажы("Доступ дазволены");
} іначай {
  скажы("Доступ забаронены");
}

nehaj vozrast = 20;

jakli (vozrast >= 18) {
  govori("Dostup je dovolen");
} inakše {
  govori("Dostup je zapreščen");
}

пусть Обещание = новый Обещание((виполни, отклони) => {
  виполни("Всё хорошо");
});

Обещание
  .затем((рез) => лог(рез))
  .лови((ошибка) => ошибка(ошибка));

  nehaj Oběčanje = novy Oběčanje((ispolni, otkloni) => {
    ispolni("Vsě je v porjadku");
  });

  Oběčanje
    .togda((rezultat) => govori(rezultat))
    .pojmi((greška) => govoriGreška(greška));

